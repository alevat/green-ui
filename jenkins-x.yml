buildPack: javascript
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        steps:
        - command: npm run build
          name: npm-run-build
    release:
      build:
        steps:
        - command: npm run build
          name: npm-run-build
